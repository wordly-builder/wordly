import {charactersFeature} from "$lib/features/characters.feature";
import {universesFeature} from "$lib/features/universes.feature";
import { worldsFeature } from '$lib/features/worlds.feature';

export const features = [
    universesFeature,
    worldsFeature,
    charactersFeature,
]