import { universesFeature } from '$lib/types/features/universes.feature';
import { worldsFeature } from '$lib/types/features/worlds.feature';
import { charactersFeature } from '$lib/types/features/characters.feature';


export const features = [
    universesFeature,
    worldsFeature,
    charactersFeature,
]